<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- FUND ALLOCATION — Form View -->
    <record id="view_fund_allocation_form" model="ir.ui.view">
        <field name="name">nn.fund.allocation.form</field>
        <field name="model">nn.fund.allocation</field>
        <field name="arch" type="xml">
            <form string="Fund Allocation">
                <header>
                    <!-- Submit: draft only, any fund user -->
                    <button name="action_submit" string="Submit for Approval"
                            type="object" class="btn-primary"
                            invisible="state != 'draft'"/>
 
                    <!-- GM Approve -->
                    <button name="action_gm_approve" string="GM Approve"
                            type="object" class="btn-primary"
                            invisible="state != 'submitted'"
                            groups="nn_fund_management.group_gm_approver,nn_fund_management.group_fund_admin"/>
 
                    <!-- MD Approve -->
                    <button name="action_md_approve" string="MD Approve"
                            type="object" class="btn-primary"
                            invisible="state != 'gm_approved'"
                            groups="nn_fund_management.group_md_approver,nn_fund_management.group_fund_admin"/>
 
                    <!-- Reject: GM or MD can reject -->
                    <button name="action_reject" string="Reject"
                            type="object" class="btn-danger"
                            invisible="state not in ('submitted','gm_approved')"
                            groups="nn_fund_management.group_gm_approver,nn_fund_management.group_md_approver,nn_fund_management.group_fund_admin"
                            confirm="Are you sure you want to reject this allocation request?"/>
 
                    <!-- Cancel: available until approved -->
                    <button name="action_cancel" string="Cancel"
                            type="object"
                            invisible="state in ('approved','rejected','cancelled')"
                            confirm="Are you sure you want to cancel this request?"/>
 
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,submitted,gm_approved,approved"/>
                </header>
 
                <sheet>
                    <!-- Reference number top-right -->
                    <div class="oe_title">
                        <label for="name" string="Request Number"/>
                        <h1>
                            <field name="name" readonly="1"/>
                        </h1>
                    </div>
 
                    <!-- Balance warning: show when about to submit -->
                    <div class="alert alert-warning"
                         invisible="state != 'draft' or not fund_account_id">
                        <i class="fa fa-info-circle"/> Submitting will lock the requested amount
                        in the fund account's hold. Ensure the amount does not exceed the
                        unassigned balance before submitting.
                    </div>
 
                    <!-- Rejected / Cancelled reason banner -->
                    <div class="alert alert-danger"
                         invisible="state not in ('rejected','cancelled') or not rejection_reason">
                        <strong>Reason: </strong>
                        <field name="rejection_reason" readonly="1" nolabel="1"/>
                    </div>
 
                    <group>
                        <group string="Allocation Details">
                            <field name="fund_account_id"
                                   options="{'no_create': True}"
                                   readonly="state != 'draft'"/>
                            <field name="project_expense_id"
                                   options="{'no_create': True}"
                                   readonly="state != 'draft'"/>
                            <field name="amount" widget="monetary"
                                   options="{'currency_field': 'currency_id'}"
                                   readonly="state != 'draft'"/>
                            <field name="currency_id" invisible="1"/>
                            <field name="purpose" readonly="state != 'draft'"/>
                        </group>
                        <group string="Request Information">
                            <field name="request_date" readonly="state != 'draft'"/>
                            <field name="requested_by" readonly="1"/>
                            <field name="company_id" groups="base.group_multi_company"
                                   readonly="1"/>
                        </group>
                    </group>
 
                    <!-- Approval Details (visible after GM action) -->
                    <group string="GM Approval" invisible="state not in ('gm_approved','approved','rejected')">
                        <field name="gm_approver_id" readonly="1"/>
                        <field name="gm_approval_date" readonly="1"/>
                    </group>
 
                    <group string="MD Approval" invisible="state != 'approved'">
                        <field name="md_approver_id" readonly="1"/>
                        <field name="md_approval_date" readonly="1"/>
                    </group>
 
                    <group string="Attachments">
                        <field name="attachment_ids" widget="many2many_binary"
                               nolabel="1" colspan="2"/>
                    </group>
 
                    <!-- Approval History tab -->
                    <notebook>
                        <page string="Approval History" name="approval_history">
                            <field name="approval_history_ids" readonly="1">
                                <list string="Approval History">
                                    <field name="date"/>
                                    <field name="approver_id"/>
                                    <field name="approval_level"/>
                                    <field name="decision" widget="badge"
                                           decoration-success="decision == 'approved'"
                                           decoration-warning="decision == 'submitted'"
                                           decoration-danger="decision in ('rejected','cancelled')"/>
                                    <field name="state_before"/>
                                    <field name="state_after"/>
                                    <field name="comment"/>
                                    <field name="amount" widget="monetary"
                                           options="{'currency_field': 'currency_id'}"/>
                                    <field name="currency_id" column_invisible="1"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- FUND ALLOCATION — List View    -->
    <record id="view_fund_allocation_list" model="ir.ui.view">
        <field name="name">nn.fund.allocation.list</field>
        <field name="model">nn.fund.allocation</field>
        <field name="arch" type="xml">
            <list string="Fund Allocations"
                  decoration-success="state == 'approved'"
                  decoration-warning="state in ('submitted','gm_approved')"
                  decoration-danger="state in ('rejected','cancelled')">
                <field name="name"/>
                <field name="request_date"/>
                <field name="fund_account_id"/>
                <field name="project_expense_id"/>
                <field name="requested_by"/>
                <field name="amount" widget="monetary"
                       options="{'currency_field': 'currency_id'}"/>
                <field name="state" widget="badge"
                       decoration-success="state == 'approved'"
                       decoration-warning="state in ('submitted','gm_approved')"
                       decoration-info="state == 'draft'"
                       decoration-danger="state in ('rejected','cancelled')"/>
                <field name="currency_id" column_invisible="1"/>
            </list>
        </field>
    </record>

    <!-- FUND ALLOCATION — Search View -->
    <record id="view_fund_allocation_search" model="ir.ui.view">
        <field name="name">nn.fund.allocation.search</field>
        <field name="model">nn.fund.allocation</field>
        <field name="arch" type="xml">
            <search string="Fund Allocations">
                <field name="name"/>
                <field name="fund_account_id"/>
                <field name="project_expense_id"/>
                <field name="requested_by"/>
                <filter string="Draft" name="draft" domain="[('state','=','draft')]"/>
                <filter string="Pending Approval" name="pending"
                        domain="[('state','in',['submitted','gm_approved'])]"/>
                <filter string="Approved" name="approved" domain="[('state','=','approved')]"/>
                <filter string="Rejected / Cancelled" name="rejected"
                        domain="[('state','in',['rejected','cancelled'])]"/>
                <separator/>
                <filter string="My Requests" name="my_requests"
                        domain="[('requested_by','=',uid)]"/>
                <group expand="0" string="Group By">
                    <filter string="Fund Account" name="grp_account"
                            context="{'group_by': 'fund_account_id'}"/>
                    <filter string="Project / Expense" name="grp_project"
                            context="{'group_by': 'project_expense_id'}"/>
                    <filter string="Status" name="grp_state"
                            context="{'group_by': 'state'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action -->
    <record id="action_fund_allocation" model="ir.actions.act_window">
        <field name="name">Fund Allocations</field>
        <field name="res_model">nn.fund.allocation</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_fund_allocation_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first Fund Allocation
            </p>
            <p>Allocate funds from a fund account to a project or expense head. Requires GM and MD approval.</p>
        </field>
    </record>
    
</odoo>
